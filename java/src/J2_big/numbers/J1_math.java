package J2_big.numbers;

public class J1_math {

    public static void calculate() {

        double number = 4.0d;
        double squareRoot = Math.sqrt(number);

        double power = Math.pow(10, 5);

        double someValue = Math.abs(-10);
        double someRandomValue = Math.random(); // >= 0 < 1

        System.out.println("Some random value: " + someRandomValue);
    }
}
